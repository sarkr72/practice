# All real configuration lives in application.yml and application-{profile}.yml.
# Kept here so an empty-properties removal doesn't break existing tooling assumptions.
#
# Select a profile via:
#   SPRING_PROFILES_ACTIVE env var      (k8s / docker)
#   -Dspring-boot.run.profiles=local    (local Maven run)
spring.profiles.default=local
